<style>
    /* ============================================
       FOOTER STYLES
       ============================================ */

    .modern-footer {
        position: relative;
        background: linear-gradient(135deg, #023859 0%, #26658C 100%);
        color: #fff;
        padding: 50px 0 20px;
        margin-top: 80px;
        overflow: hidden;
    }

    /* Animated Wave Background */
    .footer-waves {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        overflow: hidden;
        z-index: 0;
        opacity: 0.3;
    }

    .wave {
        position: absolute;
        bottom: 0;
        left: 0;
        width: 200%;
        height: 100px;
        background: linear-gradient(90deg, transparent, #54ACBF, transparent);
        animation: waveMove 8s linear infinite;
    }

    .wave:nth-child(2) {
        bottom: 10px;
        animation: waveMove 10s linear infinite reverse;
        opacity: 0.5;
        background: linear-gradient(90deg, transparent, #26658C, transparent);
    }

    .wave:nth-child(3) {
        bottom: 20px;
        animation: waveMove 12s linear infinite;
        opacity: 0.3;
        background: linear-gradient(90deg, transparent, #54ACBF, transparent);
    }

    @keyframes waveMove {
        0% {
            transform: translateX(0);
        }

        100% {
            transform: translateX(-50%);
        }
    }

    /* Floating Particles */
    .footer-particle {
        position: absolute;
        width: 4px;
        height: 4px;
        background: #54ACBF;
        border-radius: 50%;
        opacity: 0.6;
        animation: particleFloat 6s ease-in-out infinite;
    }

    .footer-particle:nth-child(1) {
        left: 10%;
        top: 20%;
        animation-delay: 0s;
    }

    .footer-particle:nth-child(2) {
        left: 30%;
        top: 40%;
        animation-delay: 1s;
    }

    .footer-particle:nth-child(3) {
        left: 50%;
        top: 60%;
        animation-delay: 2s;
    }

    .footer-particle:nth-child(4) {
        left: 70%;
        top: 30%;
        animation-delay: 1.5s;
    }

    .footer-particle:nth-child(5) {
        left: 90%;
        top: 50%;
        animation-delay: 0.5s;
    }

    @keyframes particleFloat {

        0%,
        100% {
            transform: translateY(0) scale(1);
            opacity: 0.6;
        }

        50% {
            transform: translateY(-30px) scale(1.5);
            opacity: 1;
        }
    }

    /* Footer Content */
    .footer-content {
        position: relative;
        z-index: 1;
    }

    .footer-brand {
        font-size: 1.8rem;
        font-weight: 900;
        color: #54ACBF;
        text-shadow: 0 0 10px rgba(84, 172, 191, 0.5);
        margin-bottom: 15px;
        letter-spacing: 2px;
    }

    .footer-tagline {
        color: rgba(255, 255, 255, 0.8);
        font-size: 1rem;
        margin-bottom: 30px;
    }

    /* Social Links */
    .social-links {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin-bottom: 30px;
    }

    .social-link {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        border: 2px solid rgba(84, 172, 191, 0.3);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #54ACBF;
        font-size: 1.3rem;
        transition: all 0.4s ease;
        text-decoration: none;
    }

    .social-link:hover {
        background: linear-gradient(135deg, #54ACBF 0%, #26658C 100%);
        color: #fff;
        transform: translateY(-8px) rotate(360deg);
        box-shadow: 0 10px 30px rgba(84, 172, 191, 0.5);
        border-color: #54ACBF;
    }

    /* Quick Links */
    .footer-links {
        display: flex;
        justify-content: center;
        gap: 30px;
        flex-wrap: wrap;
        margin-bottom: 25px;
    }

    .footer-link {
        color: rgba(255, 255, 255, 0.8);
        text-decoration: none;
        font-size: 0.95rem;
        font-weight: 500;
        transition: all 0.3s ease;
        position: relative;
        padding-bottom: 5px;
    }

    .footer-link::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 50%;
        transform: translateX(-50%);
        width: 0;
        height: 2px;
        background: #54ACBF;
        transition: width 0.3s ease;
    }

    .footer-link:hover {
        color: #54ACBF;
        transform: translateY(-2px);
    }

    .footer-link:hover::after {
        width: 100%;
    }

    /* Divider */
    .footer-divider {
        width: 80%;
        max-width: 600px;
        height: 2px;
        background: linear-gradient(90deg, transparent, #54ACBF, transparent);
        margin: 30px auto;
        animation: glow 3s ease-in-out infinite;
    }

    @keyframes glow {

        0%,
        100% {
            opacity: 0.3;
            box-shadow: 0 0 10px #54ACBF;
        }

        50% {
            opacity: 1;
            box-shadow: 0 0 20px #54ACBF;
        }
    }

    /* Copyright */
    .footer-copyright {
        color: rgba(255, 255, 255, 0.7);
        font-size: 0.9rem;
        margin: 0;
        position: relative;
        z-index: 1;
    }

    .footer-copyright .heart {
        color: #ff6b6b;
        animation: heartbeat 1.5s ease-in-out infinite;
        display: inline-block;
    }

    @keyframes heartbeat {

        0%,
        100% {
            transform: scale(1);
        }

        25% {
            transform: scale(1.2);
        }

        50% {
            transform: scale(1);
        }
    }

    /* Back to Top Button */
    .back-to-top {
        position: fixed;
        bottom: 30px;
        right: 30px;
        width: 50px;
        height: 50px;
        background: linear-gradient(135deg, #54ACBF 0%, #26658C 100%);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #fff;
        font-size: 1.5rem;
        text-decoration: none;
        box-shadow: 0 5px 20px rgba(84, 172, 191, 0.4);
        opacity: 0;
        visibility: hidden;
        transition: all 0.4s ease;
        z-index: 999;
    }

    .back-to-top.show {
        opacity: 1;
        visibility: visible;
    }

    .back-to-top:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 30px rgba(84, 172, 191, 0.6);
        color: #fff;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
        .modern-footer {
            padding: 40px 15px 15px;
            margin-top: 50px;
        }

        .footer-brand {
            font-size: 1.5rem;
        }

        .social-links {
            gap: 15px;
        }

        .social-link {
            width: 45px;
            height: 45px;
            font-size: 1.1rem;
        }

        .footer-links {
            gap: 20px;
        }

        .footer-link {
            font-size: 0.9rem;
        }

        .back-to-top {
            width: 45px;
            height: 45px;
            bottom: 20px;
            right: 20px;
        }
    }
</style>

<!-- Modern Footer -->
<footer class="modern-footer">
    <!-- Background Animations -->
    <div class="footer-waves">
        <div class="wave"></div>
        <div class="wave"></div>
        <div class="wave"></div>
    </div>

    <!-- Floating Particles -->
    <div class="footer-particle"></div>
    <div class="footer-particle"></div>
    <div class="footer-particle"></div>
    <div class="footer-particle"></div>
    <div class="footer-particle"></div>

    <!-- Footer Content -->
    <div class="footer-content">
        <div class="container text-center">
            <!-- Brand -->
            <h3 class="footer-brand">MOHIT RAGHAV</h3>
            <p class="footer-tagline">AI/ML Student | Web Developer | Tech Enthusiast</p>

            <!-- Social Links -->
            <div class="social-links">
                <a href="https://github.com/mohitraghav1318" target="_blank" class="social-link" title="GitHub">
                    <i class="fab fa-github"></i>
                </a>
                <a href="https://www.linkedin.com/in/mohit-raghav-45b434288" target="_blank" class="social-link"
                    title="LinkedIn">
                    <i class="fab fa-linkedin-in"></i>
                </a>
                <a href="mailto:mohitraghav1318@gmail.com" class="social-link" title="Email">
                    <i class="fas fa-envelope"></i>
                </a>
                <a href="https://twitter.com/mohitraghav1318" target="_blank" class="social-link" title="Twitter">
                    <i class="fab fa-twitter"></i>
                </a>
            </div>

            <!-- Quick Links -->
            <div class="footer-links">
                <a href="/" class="footer-link">Home</a>
                <a href="/projects" class="footer-link">Projects</a>
                <a href="/skills" class="footer-link">Skills</a>
                <a href="/contact" class="footer-link">Contact</a>
            </div>

            <!-- Divider -->
            <div class="footer-divider"></div>

            <!-- Copyright -->
            <p class="footer-copyright">
                © 2025 Mohit Raghav | Designed & Developed with Passion

        </div>
    </div>
</footer>

<!-- Back to Top Button -->
<a href="#" class="back-to-top" id="backToTop">
    <i class="fas fa-arrow-up"></i>
</a>

<!-- Font Awesome -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

<!-- Bootstrap Bundle JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
    crossorigin="anonymous"></script>

<!-- Back to Top Button Script -->
<script>
    // Back to Top Button Functionality
    const backToTopButton = document.getElementById('backToTop');

    window.addEventListener('scroll', () => {
        if (window.pageYOffset > 300) {
            backToTopButton.classList.add('show');
        } else {
            backToTopButton.classList.remove('show');
        }
    });

    backToTopButton.addEventListener('click', (e) => {
        e.preventDefault();
        window.scrollTo({
            top: 0,
            behavior: 'smooth'
        });
    });

    // Add smooth scroll to all anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            const href = this.getAttribute('href');
            if (href !== '#') {
                e.preventDefault();
                const target = document.querySelector(href);
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth'
                    });
                }
            }
        });
    });
</script>

<script src="app.js"></script>
</body>

</html>